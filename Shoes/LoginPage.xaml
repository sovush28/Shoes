<Page x:Class="Shoes.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Shoes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage"
      FontFamily="Times New Roman"
      IsVisibleChanged="Page_IsVisibleChanged">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="390"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Логин: " FontSize="20" Grid.Row="0" Grid.Column="0"
                   HorizontalAlignment="Right" VerticalAlignment="Center"/>
        <TextBox Name="LoginTB" Grid.Column="1"
                 Margin="10" Width="250" Height="30" FontSize="20"
                 HorizontalAlignment="Left" />
        <TextBlock Text="Пароль: " FontSize="20" Grid.Row="1" Grid.Column="0"
                   HorizontalAlignment="Right" VerticalAlignment="Center"/>
        <TextBox Name="PasswTB" Grid.Column="1"
                 Margin="10" HorizontalAlignment="Left"
                 Width="250" Height="30" FontSize="20"
                 Grid.Row="1"/>
        <Button Name="LoginBtn" Content="Войти"
                HorizontalAlignment="Center" Grid.ColumnSpan="2"
                Click="LoginBtn_Click"
                Grid.Row="2"/>
        <Button Name="LoginGuestBtn" Content="Войти как гость"
                HorizontalAlignment="Center" Grid.ColumnSpan="2"
                Click="LoginGuestBtn_Click"
                Grid.Row="3"/>
    </Grid>
</Page>
